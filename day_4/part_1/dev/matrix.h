/*
    we use an interface file to construct
    the structure of the data type
*/
/*
    I want to create somthing like:
    Board[i] = 
*/

typedef struct {
    int* all_rows[5];
    int* all_columns[5];
} Board;

